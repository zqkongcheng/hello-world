异步：发出ajax请求后无需等待，后面代码会直接执行。比如，页面加载时，加载了名字叫001的ajax、002的函数、003的函数，此时001发出异步请求，002和003无需等001执行完，便可直接执行。好处是用户无需等待，提升界面交互性。

同步：如上例子，001如果发出同步请求，002和003必须等待001执行完，才能执行，001从发出请求到接收响应再到处理数据需要时间，也就是，需要等待，用户体验没有异步好。

同步好处：当002执行时，需要用到001获得的数据，这时候如果使用异步，002在001未执行之前就执行了，那也就是拿不到数据，数据就为null了。而同步是002必须等到001执行完才能执行，也就避免了这种数据为null的问题。